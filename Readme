MARKING LATITUDE and LONGITUDE in google Map 

Create a django project
Update the API key using https://developers.google.com/maps/documentation/javascript/get-api-key
Make sure to give your API-Key in settings.py and templetes(index.html) instead of "YOURAPIKEY"
Check the console if the page doesnot loads and go to https://churchthemes.com/page-didnt-load-google-maps-correctly/
Change lat and long in HTML as per ur need

